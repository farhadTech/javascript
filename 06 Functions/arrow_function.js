/* An arrow function is essentially an anonymous function with a shorter syntax. Thery are often assigned to variables, making them reusabel. Arrow functions are also know as lambda functions in some other programming languages.
*/
/*
// 1. Arrow function without parameters.
const gfg = () => {
  console.log("Hi from GeeksforGeeks!");
}
gfg();

// 2. Arrow function with single parameters.
const square = x => x * x;
console.log(square(4));

// 3. Arrow function with multiple parameters.
const gfg2 = (x, y, z) => {
  console.log(x + y + z);
}
gfg2(10, 20, 30);

// 4. Arrow function with default parameters.
const gfg3 = (x, y, z = 30) => {
  console.log(x + " " + y + " " + z);
}
gfg3(10, 20);

// 5. Return object literals, () => ({ key: value })
const makePerson = (firstName, lastName) => ({ first: firstName, last: lastName });
console.log(makePerson("Pankaj", "Bind"));
*/